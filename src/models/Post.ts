export class Post {
    constructor(public title: string, public body: string, public author: string, public placeAddress: string, public imgUrl: string, public price: string, public id?: string) {
        
    }
}